<Window x:Class="LibrarySystem.ViewRecordsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Transaction History" Height="600" Width="900" WindowStartupLocation="CenterScreen">
	<Grid Margin="15">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Text="ISSUED &amp; RETURNED RECORDS" FontSize="20" FontWeight="Bold" Foreground="#FF1A2A6C" HorizontalAlignment="Center" Margin="0,0,0,15"/>

		<StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
			<TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,10,0"/>
			<TextBox x:Name="txtSearch" Width="250" Height="30" VerticalContentAlignment="Center" TextChanged="txtSearch_TextChanged"/>
			<TextBlock Text=" (Search by Name or Book)" Foreground="Gray" VerticalAlignment="Center" Margin="10,0"/>
		</StackPanel>

		<DataGrid Grid.Row="2" x:Name="dgLogs" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" AlternatingRowBackground="#F2F2F2">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Borrower Name" Binding="{Binding Name}" Width="*"/>
				<DataGridTextColumn Header="Book Title" Binding="{Binding Book}" Width="*"/>
				<DataGridTextColumn Header="Issue Date" Binding="{Binding IDate, StringFormat=d}" Width="120"/>
				<DataGridTextColumn Header="Due/Return Date" Binding="{Binding RDate, StringFormat=d}" Width="120"/>
				<DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</Window>